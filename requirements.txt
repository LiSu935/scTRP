# Core (always required)
torch>=1.13.0
torchvision>=0.14.0
numpy>=1.23
pyyaml>=6.0
scipy>=1.9
scikit-learn>=1.1
anndata>=0.9
tqdm>=4.64
pot>=0.9           # Python Optimal Transport (POT)

# Light XGBoost alternative (no GPU needed)
xgboost>=1.7
joblib>=1.2
matplotlib>=3.6

# Training & data loading (full scTRP pipeline)
wandb>=0.15
webdataset>=0.2
fair-esm>=2.0.0
flash-attn<1.0.5
transformers>=4.32.1
scanpy>=1.9
muon>=0.1
scirpy>=0.12

# scGPT — install from source (see README)
# git clone https://github.com/bowang-lab/scGPT
# pip install -e /path/to/scGPT
